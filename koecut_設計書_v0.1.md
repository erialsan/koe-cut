# NarraEdit — 初期設計書 v0.1

> Linux向け・オープンソース 汎用電子ナレーション統合動画編集ソフトウェア

---

## 1. プロジェクト概要

| 項目 | 内容 |
|---|---|
| プロジェクト名（仮） | NarraEdit |
| ターゲットOS | Linux（主要ディストリビューション対応） |
| ライセンス | OSS（Apache 2.0 または MIT、要検討） |
| 開発言語 | Rust |
| UIフレームワーク | 未決定（候補：egui / GTK4+Relm4 / Tauri） |
| 動画処理バックエンド | FFmpeg（ライブラリ連携） |

---

## 2. コア機能定義

### 2.1 動画編集（MVP必須）

- **タイムライン編集**：映像・音声・字幕トラックの配置・移動・トリム
- **プレビュー再生**：編集内容のリアルタイムプレビュー
- **プロジェクトファイル保存・読み込み**：独自形式（JSON or TOML）で状態保存

### 2.2 電子ナレーション機能

- **TTS（テキスト音声変換）**：複数エンジン対応（後述）
- **字幕・テロップ自動生成**：TTS出力またはWhisperによる音声認識から自動生成、SRT/ASS出力
- **多言語対応**：エンジン側の対応言語をそのまま活用、UIは日本語・英語対応

---

## 3. TTSエンジン対応設計

プラグインアーキテクチャ（トレイト抽象化）により、エンジンを差し替え可能にする。

```
TTSEngine（trait）
├── VOICEVOX        → ローカルHTTP API（http://localhost:50021）
├── AquesTalk       → CLIバイナリラッパー（ライセンス別途）
├── CoeFont         → クラウドREST API（APIキー設定）
├── Qwen3-TTS       → ローカル推論（ONNX Runtime または Pythonサイドカー）
└── RVC             → 後段の声質変換レイヤー（どのエンジン出力にも適用可能）
```

### エンジン選択方針

| エンジン | 動作形態 | オフライン | 商用利用 |
|---|---|---|---|
| VOICEVOX | ローカルAPI | ✅ | 条件付き（規約確認） |
| AquesTalk | CLIラッパー | ✅ | 要ライセンス購入 |
| CoeFont | クラウドAPI | ❌ | 要API契約 |
| Qwen3-TTS | ローカル推論 | ✅ | Apache 2.0 |
| RVC | ローカル処理 | ✅ | モデル依存 |

---

## 4. アーキテクチャ概要

```
┌─────────────────────────────────────────────┐
│                   GUI レイヤー                │
│        （egui / GTK4+Relm4 / Tauri）         │
│   タイムライン UI ／ プレビュー ／ 設定画面   │
└────────────────────┬────────────────────────┘
                     │ イベント / コマンド
┌────────────────────▼────────────────────────┐
│               コアエンジン（Rust）            │
│  ┌──────────┐ ┌──────────┐ ┌─────────────┐ │
│  │ Project  │ │ Timeline │ │  Renderer   │ │
│  │ Manager  │ │  Engine  │ │ (FFmpeg)    │ │
│  └──────────┘ └──────────┘ └─────────────┘ │
│  ┌──────────────────────────────────────┐   │
│  │         TTS Manager                  │   │
│  │  VOICEVOX / AquesTalk / CoeFont /    │   │
│  │  Qwen3-TTS  →  [RVC レイヤー]        │   │
│  └──────────────────────────────────────┘   │
│  ┌──────────────────────────────────────┐   │
│  │       字幕エンジン                    │   │
│  │  Whisper（音声→字幕） / SRT・ASS出力  │   │
│  └──────────────────────────────────────┘   │
└─────────────────────────────────────────────┘
```

---

## 5. モジュール構成（Rustクレート設計）

```
narraEdit/
├── crates/
│   ├── core/            # ビジネスロジック（UI非依存）
│   │   ├── project/     # プロジェクト管理・シリアライズ
│   │   ├── timeline/    # タイムラインデータ構造
│   │   ├── renderer/    # FFmpeg連携・エクスポート
│   │   ├── tts/         # TTSトレイト・各エンジン実装
│   │   ├── subtitle/    # 字幕生成・Whisper連携
│   │   └── rvc/         # RVC声質変換
│   └── app/             # GUIアプリ本体
│       ├── ui/          # 画面・ウィジェット
│       └── main.rs
├── Cargo.toml
└── README.md
```

---

## 6. GUIフレームワーク選定（要決定）

| 候補 | メリット | デメリット |
|---|---|---|
| **egui** | 純Rust・軽量・クロスプラットフォーム | タイムラインUIのカスタム描画が必要 |
| **GTK4 + Relm4** | Linuxネイティブ・GNOMEとの親和性 | Rust bindingsの成熟度がやや低い |
| **Tauri** | WebUI（TypeScript）でリッチなUI | Rustのみで完結しない |

**推奨：** タイムラインUIのカスタム制御が必要なため、**egui** をベースに `egui_extras` や独自ウィジェットを組み合わせる方向が最もRustらしく管理しやすい。

---

## 7. プロジェクトファイル形式

- 拡張子：`.nredit`（内部はJSON or TOML）
- 管理対象：タイムライン状態・トラック情報・TTSスクリプト・字幕データ・エンジン設定

---

## 8. 開発ロードマップ（フェーズ案）

| フェーズ | 内容 | 目安 |
|---|---|---|
| **Phase 0** | 設計確定・リポジトリ構築・CI設定 | 1〜2週 |
| **Phase 1** | コアデータ構造・プロジェクト保存・FFmpeg連携 | 3〜4週 |
| **Phase 2** | 基本タイムラインUI・プレビュー再生 | 4〜6週 |
| **Phase 3** | VOICEVOX・Qwen3-TTS連携・字幕自動生成 | 4週 |
| **Phase 4** | AquesTalk・CoeFont・RVC対応 | 3〜4週 |
| **Phase 5** | 多言語UI・Whisper統合・エクスポート機能 | 3週 |
| **Phase 6** | OSS公開・ドキュメント整備・β版リリース | 2週 |

---

## 9. 技術的留意点・リスク

- **VOICEVOX**：ローカルで起動している前提。アプリからの自動起動も将来的に検討。
- **AquesTalk**：バイナリ配布のライセンス制約あり。OSS本体には含めず、ユーザーが別途用意する形を推奨。
- **Qwen3-TTS**：推論リソース（GPU/CPU）が重い。ONNXまたはllama.cpp経由での軽量化を検討。
- **RVC**：PythonエコシステムへのブリッジまたはONNX変換が必要。Rustからのサイドカープロセス管理が現実的。
- **Whisper**：`whisper-rs`（Rustバインディング）または`faster-whisper`（Pythonサイドカー）を選択。
- **タイムラインUI**：動画編集のUIは実装コストが高い。eguiの場合、初期はシンプルな構成から始めて段階的に拡張する。

---

*本書はv0.1初期設計案です。フレームワーク選定・詳細仕様は今後のフェーズで確定します。*
